spring:
  application:
    name: uploadextrahours-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:5432/uploadextrahours
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update