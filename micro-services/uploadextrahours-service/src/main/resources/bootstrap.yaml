spring:
  application:
    name: uploadextrahours-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/uploadextrahours
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update