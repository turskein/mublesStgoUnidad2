server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/turskein/mublesStgoUnidad2
          search-paths: 'micro-services/config-data'
  application:
    name: config-service